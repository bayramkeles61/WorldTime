[build.environment]
  NPM_FLAGS = "--version"
  NODE_VERSION = "16"

[build]
  publish = "dist"
  command = "npx npm i --store=node_modules/.npm-store && npx npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200